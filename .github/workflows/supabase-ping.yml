name: Supabase Keep Alive

on:
  schedule:
    - cron: '0 0 */3 * *'  # 每 3 天执行一次
  workflow_dispatch:      # 允许手动测试

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase to keep project alive
        run: |
          curl -s --max-time 10 "https://rrmghbykyvflxqwvgqhu.supabase.co/rest/v1/?apikey=anon" || true
