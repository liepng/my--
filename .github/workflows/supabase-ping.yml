name: Supabase Keep Alive

on:
  schedule:
    - cron: '0 0 */3 * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Keep Supabase Project Alive
        run: curl -s --max-time 10 -H "apikey: sb_publishable_ebGMg1GJQQuUkfnRNzxCFA_Bfy1iPWV" "https://rrmghbykyvflxqwvgqhu.supabase.co/auth/v1/.well-known/jwks.json" || true
